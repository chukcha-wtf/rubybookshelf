<h2>
  {{book.title}}
  {{#if editing}}
    <a href="#" class="pull-right btn btn-success" {{action 'save'}}>Save</a>
    <a href="#" class="pull-right btn btn-warning" {{action 'cancel'}}>Cancel</a>
  {{else}}
    {{#canEditRecord book}}
      <a href="#" class="pull-right btn btn-info" {{action 'edit'}}>Edit</a>
      <a href="#" class="pull-right btn btn-danger" {{action 'delete'}}>Delete</a>
    {{/canEditRecord}}
  {{/if}}
</h2>

<div>
  <dl class="dl-horizontal">
    <dt>Title</dt>
    <dd>
      {{#if editing}}
        {{input type="text" value=book.title class="form-control"}}
      {{else}}
        {{book.title}}
      {{/if}}
    </dd>
    <dt>Description</dt>
    <dd>
      {{#if editing}}
        {{textarea value=book.description class="form-control"}}
      {{else}}
        {{book.description}}
      {{/if}}
    </dd>
    <dt>Author</dt>
    <dd>
      {{#if editing}}
        {{view Ember.Select content=authors optionValuePath="content.id" optionLabelPath="content.fullname" value=book.author.id class="form-control" }}
      {{else}}
        {{#link-to 'author' book.author}}{{book.author.fullname}}{{/link-to}}
      {{/if}}
    </dd>
    <dt>User</dt>
    <dd>{{#link-to 'user' book.user}}{{book.user.fullname}}{{/link-to}}</dd>
  </dl>
</div>